#!/bin/env node

'use strict';
/**
 * @file Launch the server
 *
 */


// Load configuration
var configuration = require("../config/configuration.js");
var server = require('../app.js');

// Start the server
server.listen(configuration.port, function() {
  'use strict';
  console.log("Provider dropbox started at " + server.url);
});
